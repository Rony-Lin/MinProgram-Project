<!--components/w-goods-item/w-goods-item.wxml-->
<view class="goods-item"  bindtap='itemClick'>
  <image class="image" src="{{ goodsItem.image|| goodsItem.img || goodsItem.show.img }}" mode="widthFix"></image>
  <view class="desc-info">
    <view class="title">{{goodsItem.title}}</view>
    <view class="info">
        <text class="price">￥{{goodsItem.price}}</text>
        <image class="icon" src="/assets/common/favor.png"></image>
        <text class="cfav">{{goodsItem.cfav}}</text>
    </view>
  </view>
</view>
